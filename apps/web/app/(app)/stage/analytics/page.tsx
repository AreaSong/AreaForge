import Link from "next/link";
import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/auth/session";
import { getAnalyticsSummary } from "@/lib/study/analytics-service";

export const dynamic = "force-dynamic";
export default async function StageAnalyticsPage({ searchParams }: { searchParams: Promise<{ window?: string }> }) { const user = await getCurrentUser(); if (!user) redirect("/login"); const query = await searchParams; const window = query.window === "30" ? 30 : 7; const analytics = await getAnalyticsSummary(new Date(), user.id, window); return <section className="space-y-5"><div><p className="text-sm text-teal-300">阶段</p><h1 className="text-2xl font-semibold text-white">{window} 天趋势</h1></div><div className="flex gap-2"><Link href="/stage/analytics?window=7" className="rounded-md border border-white/10 px-3 py-2 text-sm">7 天</Link><Link href="/stage/analytics?window=30" className="rounded-md border border-white/10 px-3 py-2 text-sm">30 天</Link></div><div className="grid gap-3 sm:grid-cols-3"><div className="rounded-md border border-white/10 bg-[#101419] p-4"><p className="text-xs text-zinc-500">有效投入</p><p className="mt-2 text-xl text-white">{analytics.totals.weekEffectiveMinutes} 分</p></div><div className="rounded-md border border-white/10 bg-[#101419] p-4"><p className="text-xs text-zinc-500">任务完成率</p><p className="mt-2 text-xl text-white">{Math.round(analytics.totals.weeklyTaskCompletionRate * 100)}%</p></div><div className="rounded-md border border-white/10 bg-[#101419] p-4"><p className="text-xs text-zinc-500">薄弱节点</p><p className="mt-2 text-xl text-white">{analytics.totals.weakNodeCount}</p></div></div><p className="text-xs text-zinc-500">legacy fallback 模拟只进入历史趋势，不提升为结构化阶段建议。</p></section>; }
