interface:
  display_name: "AreaForge Supply Chain"
  short_description: "Release artifact trust and dependency gates"
  default_prompt: "Use $areaforge-supply-chain to verify AreaForge release artifacts, image digests, signatures, dependencies, and updater trust."
