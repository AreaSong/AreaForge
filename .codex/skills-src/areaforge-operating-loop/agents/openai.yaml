interface:
  display_name: "AreaForge Operating Loop"
  short_description: "Route tasks through AreaForge owner skills"
  default_prompt: "Use $areaforge-operating-loop to classify AreaForge work, choose owner skills, verify evidence, and close the loop with the latest local test-pool slot and URL when applicable."
