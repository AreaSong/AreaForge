interface:
  display_name: "AreaForge Validation Driver"
  short_description: "Choose and report sufficient checks"
  default_prompt: "Use $areaforge-validation-driver to choose and report the right AreaForge validation gates, reusing the latest local test-pool runtime and cleaning one-shot browser containers when applicable."
