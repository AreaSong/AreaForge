interface:
  display_name: "AreaForge QA Smoke"
  short_description: "End-to-end user smoke evidence"
  default_prompt: "Use $areaforge-qa-smoke to verify an AreaForge user journey; for local UI work use the machine-reported latest test-pool URL and reuse one runtime, while production smoke uses the production read-only URL and limits. Clean any one-shot browser container when applicable."
