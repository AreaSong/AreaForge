# v1.2 高密度工作台与全局状态体验

## 当前状态

- 当前版本状态：**Release 已发布，生产待确认**（`v1.2.0` 第一阶段已于 2026-09-01 完成，第二阶段 Release workflow 已成功）。
- 是否已有线上 Release：是，`v1.2.0`；生产与回滚基线仍为 `v1.1.1`，production apply 尚未执行。
- 是否需要同步 `docs/**`、`tasks/**`、`workflow/**` 和入口 README：是。

以下 Planning Gate 保留第一阶段的授权边界；第二阶段 Release 资产与 strict 供应链证据以 `docs/development/release-supply-chain-v1.2.0.md` 为准，生产 apply 仍未授权。

## 目标

将 `feature/ui-optimization` 中已经完成的高密度工作台、Dynamic Island、响应式共享治理、错题 v2 与相关 API 收口为可进入 PR/CI 的 `v1.2.0` 候选，在不触碰生产的前提下形成可验证、可回退、可继续进入签名 Release 的干净默认分支 commit。

## Planning Gate

- 目标：统一版本与源事实，验证 36 条 migration、完整源代码门禁和桌面/移动体验，经 PR CI 后 squash 合并到 `main`。
- 非目标：创建或推送 tag、创建 GitHub Release、推送 GHCR 镜像、生产 migration、updater apply、backup/restore、rollback、自动应用策略变化或 residual 状态变更。
- Exact docs：`CHANGELOG.md`、`docs/development/high-risk-confirmation-packets.md`、`docs/development/validation-matrix.md`、`docs/development/operational-readiness.md`、`docs/development/feature-traceability.md`、`tasks/active/0036-responsive-layout-system.md`、`tasks/done/0037-v12-release-preparation.md`。
- Open questions：后续独立确认时，是否在满足生产 release train 条件后执行 `v1.2.0` production apply；本阶段不改变自动应用策略或 residual 状态。
- Decisions：版本采用 `v1.2.0`；通过 PR 触发 CI，成功后 squash 合并；本阶段只允许在一次性 PostgreSQL 16 隔离环境验证 additive migration。
- Owner skill：`areaforge-release-operator` 主导；`areaforge-supply-chain`、`areaforge-security-governance`、`areaforge-ai-governance`、`areaforge-file-storage-safety`、`areaforge-enterprise-governance`、`areaforge-product-experience`、`areaforge-qa-smoke`、`areaforge-validation-driver`、`areaforge-doc-sync` 协同。
- Validation profile：full release candidate + isolated migration + production-build browser review。
- Source docs：`ORIGINAL_REQUEST.md`、`PROJECT.md`、`TEST_READY.md`、`docs/product/charter.md`、`docs/ux/application-shell-and-workbench-layouts.md`、`docs/development/feature-traceability.md`。
- Source baseline：`feature/ui-optimization` tip `fd134386576a11bcae8c5038cf13f3e989258ae6`；第一阶段产品代码 squash commit 为 `c8b5acf241bcaada59c6b469fd562d4fe400d521`，PR #51 随后完成状态文档收口。该 commit 是产品代码证据，不预先冻结后续 tag 目标。
- Residual risk IDs：`AF-RISK-SC-001`、`AF-RISK-SC-002`、`AF-RISK-UX-001`、`AF-RISK-DATA-001`、`AF-RISK-REL-001`；本阶段不修改其状态。
- Release trigger：已在第二阶段从 fresh readback 的 `main` commit 创建 `v1.2.0` tag 并触发 workflow；后续 production apply 仍需独立确认。
- Apply boundary：Release 资产已发布；生产 migration、updater apply、backup/restore、rollback 和写入型 smoke 仍保持禁用，生产保持 R0。
- Evidence freshness：所有本地命令、测试池 fingerprint、浏览器矩阵和 CI 必须绑定当前候选；旧截图只能作历史参考。
- 验证：additive migration apply/repeat、`pnpm check`、Release/治理/供应链/文档门禁、`pnpm dev:test:refresh`、latest URL 桌面/移动浏览器矩阵、PR CI。
- 回滚：PR 前保持 `main` 不变；合并后、tag 前使用普通 revert 恢复 squash commit。additive migration 未在生产执行，不需要生产数据库或上传目录恢复。

## 范围

- 高密度工作台与 8pt 视觉节奏、容器查询响应式布局、工具栏压缩和卡片层级降噪。
- Dynamic Island 状态胶囊、活动切换、命令面板、焦点恢复与 reduced-motion 行为。
- 错题 v2 的题面、答案、原因、作答记录、笔记关联和模拟失分来源 additive schema/API/UI。
- Web API/DTO/UI/浏览器状态边界门禁及 CI 接入。
- `v1.2.0` package version、CHANGELOG、任务、版本计划和状态入口同步。

## 不包含

- 生产 migration、数据回填、旧字段删除、附件删除或上传目录操作。
- AI payload 扩大、真实 Provider key smoke、AI history/token/cost/provider trace 持久化。
- tag、GitHub Release、GHCR 镜像、生产 updater apply、backup/restore、rollback 或 auto-apply 变化。

## 入口条件

- 用户已明确确认第一阶段范围。
- `feature/ui-optimization` 相对 `main` 为单向领先且工作区干净。
- GitHub CLI HTTPS 凭据可用；PR 前本地验证必须通过。

## 验收标准

- 功能证据：高密度核心工作台、Dynamic Island、错题 v2 与既有学习闭环无回归。
- 文档同步：版本、候选、稳定 Release 和生产基线表述一致，不能把候选写成已发布或已部署。
- 验证命令：任务文件列出的完整本地门禁、测试池与浏览器命令通过。
- 发布证据：Release workflow run `33521890241` 的 validate/build 成功；签名 Release 资产、strict supply-chain record 和 immutable digest 见 `docs/development/release-supply-chain-v1.2.0.md`。
- 残余风险：保留并报告上述 residual ID，不自动重新打开或关闭。

## 退出条件

- PR #49 的 CI run `33505174259` 成功并 squash 合并；main push CI run `33506280124` 成功，随后 fresh readback 确认为 `018cdfaa7a58cea2b32a33acaa0b968f29b9e09a`。
- annotated tag `v1.2.0` 已指向该 commit，Release workflow run `33521890241` 的 validate/build job 成功，Release 已发布；资产与严格校验记录见 `docs/development/release-supply-chain-v1.2.0.md`。
- 未触碰生产，未执行自动应用策略或 residual 状态变更；production-evidence 保持 pending。

## 运维边界

- Web runtime 不直接执行 Docker、备份、恢复、migration 或服务器命令。
- 自动更新只能通过受控请求或服务器侧 updater 执行；本阶段不提交更新请求。

## 风险

- 影响：964 个文件、36 条 migration、核心 Web 工作台、API、测试与 CI 门禁进入同一候选。
- 风险：migration SQL/Prisma 漂移、页面或焦点回归、测试证据过期、CI 新门禁失败、版本与默认分支身份不一致。
- 验证：隔离 PostgreSQL apply/repeat、完整 `pnpm check`、release/governance/supply-chain gates、测试池与桌面/移动浏览器矩阵、GitHub CI。
- 回滚：合并前不改变 `main`；合并后 tag 前 revert squash commit。生产未触碰，禁止把本地 additive migration 结果解释为生产可回滚证据。
