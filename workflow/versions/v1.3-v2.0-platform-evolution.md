# v1.3-v2.0 A -> B 平台演进

## 当前状态

- 当前计划状态：已确认总体方向，M0 已完成，v1.3 本地实现与受保护 PR/CI 已完成；v1.4 AUTH-0 设计与确认包已形成，v1.4-v2.0 runtime 尚未开始。
- 当前生产：`v1.1.1`；最新稳定 GitHub Release：`v1.2.0`；v1.3 已由 PR #56 squash 合并到 `main` commit `40f1b36780418bbd544aaaadcd29c385aa2154e8`。
- v1.3 已完成当前源码空库完整旅程、桌面/移动本地验收、最终本地总门禁和受保护 PR/CI；尚缺签名 Release 和独立 production apply，这些状态不能由已有本地代码、浏览器或 CI PASS 代替。
- 本计划不把规划、隔离实现、Release 或 production apply 混为同一完成状态。

## 目标

在不破坏个人长期备考闭环的前提下，把 AreaForge 从单管理员私有应用逐步演进为邀请制多人学习平台。最终同时具备个人 Workspace、多用户成员关系、统一权限、可验证的数据生命周期、受控运维、主动加入的挑战排名和多人平台运营能力。

## 执行顺序与状态口径

```text
v1.3 动态个人版
  -> v1.4 身份 / Workspace / Membership
  -> v1.5 RBAC / 隐私授权 / Coach 协作
  -> v1.6 导出 / 回收站 / 物理删除
  -> v1.7 受控运维中心
  -> v1.8 成长指标 / 私有挑战 / 排名
  -> v1.9 平台化加固
  -> v2.0 综合发布门禁
```

- `规划完成`：范围、依赖、风险、验收和回滚已写清，不代表有 runtime。
- `本地实现`：代码已存在，但尚未完成本轮全部专项验证。
- `本地验证`：专项测试、真实 API/浏览器旅程和迁移 fixture 已通过，不代表已发布。
- `Release 完成`：受保护 PR/CI 和签名 Release 制品已验证，不代表生产已更新。
- `生产完成`：独立 production apply、smoke、观察和回滚证据齐全。
- 版本按上述顺序发布；低风险设计和只读清查可以提前并行，但不能绕过前置版本或高风险确认开始写入实现。

## 下一轮执行清单

1. `V13-01 动态目录`（已完成）：阶段模板、共享任务类型、共享资料分类及消费者契约测试。
2. `V13-02 科目合并`（已实现、专项已通过）：preview/confirm/undo、全引用事务迁移、软归档、24 小时撤销和隔离 PostgreSQL 负向验证。
3. `V13-03 当前源码体验验收`（本地已完成）：共享测试池已刷新到 slot 1 / `http://127.0.0.1:43171` / source `sha256:32da0af477aa...`；阶段模板、任务类型、资料分类、科目管理及重复 preview/confirm/undo 已完成真实浏览器验收，390×844 与 1440×900 页面无横向溢出。
4. `V13-04 空库完整旅程`（本地已完成）：隔离数据库未选择任何考试模板，从零创建自定义工作区、分组、唯一科目、可编辑阶段模板和关联任务；模板提交前 `StagePlan=0`，随后完成专注收口、知识卡片证据、每日复盘并在今日页读回“1 段有效学习 / 1 条证据 / 今日已闭环”。目标考试日编辑后保存并按上海日期 `2027-12-20` 读回。
5. `V13-05 最终门禁`（本地已完成）：Core 109/109、Web 914/914、Core/Web/专项 typecheck、Web lint 0 error、全新隔离数据库 10 组 runtime selftest、`pnpm check`、docs/risk/governance/tasks gate 和 `git diff --check` 全部通过；lint 保留 200 个既有 warning。
6. `V13-06 Git 检查点与 PR/CI`（已完成）：用户 `output/playwright/*.png` 未提交；PR #56 两个 `ci/verify` 通过并 squash 合并到 `main` commit `40f1b36780418bbd544aaaadcd29c385aa2154e8`，main push CI run `33906942919` 成功。
7. `V13-07 Release`（独立阶段、待确认）：另行准备版本冻结、签名 Release 与供应链证据。
8. `V13-08 Production apply`（高风险、未授权）：备份、migration/apply、smoke、观察和回滚需独立生产确认。
9. `V14-00 AUTH 冻结`（设计已完成、待精确确认）：威胁模型、数据归属、migration preimage、会话/邀请策略、SMTP 准入、验证和回滚已写入精确确认包；确认前不实现认证或 Membership migration。

## Planning Gate

- 目标：A（个人完整产品）稳定后渐进到 B（多人学习平台）。
- 非目标：通用团队项目管理、任意 shell、默认公开排名、企业组织树、SSO/SCIM、计费、小程序、原生 App、AI 自动应用完整计划。
- Exact docs：`docs/product/charter.md`、`docs/product/feature-scope.md`、`docs/product/roadmap.md`、`docs/development/feature-traceability.md`、本文件。
- Open questions：各版本冻结时确定精确 migration、API、页面、角色矩阵、保留期和生产窗口；未冻结项不能凭总体确认直接推断实现细节。
- Decisions：个人路径优先；邀请制先于公开注册；预设角色先于自定义权限；导出先于物理删除；私有挑战先于公开排行；operation intent 取代任意 shell。
- Owner skill：`areaforge-operating-loop` 统筹；产品、安全、文件、SRE、供应链、验证和文档由对应 owner skill 负责。
- Validation profile：每批按风险选择 core/Web/Prisma/API/浏览器/跨租户/文件/ops/Release/生产证据，不以 `pnpm check` 单独证明高风险能力。
- Source baseline：个人学习闭环、Package A-E、`v1.2.0` Release 和当前分支科目 CRUD/闭环增强。
- Residual risk IDs：`AF-RISK-DATA-001`、`AF-RISK-DATA-002`、`AF-RISK-DATA-003`、`AF-RISK-OPS-009`；新稳定风险只在 owner、关闭条件和复核周期明确后登记。
- Release trigger：每个可部署版本独立受保护 PR、CI、签名 Release 和 production apply 确认。
- Apply boundary：Web runtime 不直接执行服务器命令；migration、删除、备份、恢复、updater apply 和 rollback 均需对应高风险确认。
- 回滚：additive-first；先回滚应用，保留兼容字段/表；数据删除和生产操作按各包备份、撤销或恢复策略执行。

## 不变产品契约

1. 个人用户无需邀请成员、配置权限或参加排名即可完成全部学习闭环。
2. 学习源事实归用户和 Workspace 所有；排名、通知、搜索和 AI 都是派生消费者。
3. 敏感内容默认私有，分享必须显式、最小化、可审计和可撤销。
4. 建议、草稿、确认和应用保持分层，不因 Coach、Admin 或排行自动改写正式记录。
5. 所有私有 API 在服务端授权；隐藏客户端按钮不构成权限控制。
6. Web runtime 不持有 root、Docker socket、生产 secrets、备份目录或自由命令能力。

## M0：基线归一

### 范围

- 修复当前分支依赖审计，完成受保护 PR 和 CI。
- 统一 checkout、最新 Release、生产版本三层事实。
- 清理任务状态、功能追踪和历史叙事漂移；历史证据不改写，只标明历史边界。
- 决定 `v1.2.0` 独立 production apply，或记录跳过并冻结下一候选；两者都需独立生产确认。

### 退出条件

- `pnpm audit:all`、`pnpm audit:prod`、`pnpm governance:preflight`、`pnpm check` 通过。
- 当前分支已有 PR，required check 通过并按保护规则合并。
- 路线、任务、追踪和版本事实一致。

### 当前完成证据

- PR #54 已通过两个 required `verify`，并 squash 合并到 `main` 合并提交 `7720177d9ab691d509004ce8c8201e631f84be0f`。
- main push CI run `33879794378` 已通过依赖审计、治理、残余、文档和完整 `Check`；M0 任务已归档到 `tasks/done/0038-platform-evolution-m0.md`。
- M0 只关闭基线归一，不关闭 v1.3-v2.0 runtime，也不授权生产操作。

## v1.3：个人版完全动态化

### 当前本地进度

- 已实现：多自定义科目/分组首次设置、版本化通用模板目录、408 运行时去特殊化、科目/分组 CRUD、相邻排序、归档/恢复、重复集合识别、业务引用与结构冲突预览。
- 已实现：六个版本化阶段模板作为可编辑表单种子；任务类型和八类资料分类已收敛到 `packages/core` 共享目录，创建 API、编辑兼容和各 UI 消费同一源，不再由页面各自写死。
- 已实现：严格 preview/confirm/undo 命令，snapshot/revision/幂等校验、活动 session 与不可判定唯一冲突阻断、单事务全引用迁移、知识点关联确定性去重、模拟补救来源重建、来源科目软归档和 24 小时精确撤销。撤销只恢复该次迁移的科目归属及必要来源 identity；其他业务字段保留，映射漂移、过期或唯一冲突整体拒绝。
- 已验证：全新临时 PostgreSQL 17 应用当前 36 个 migration 后，`AREAFORGE_V13_SUBJECT_MERGE_ISOLATED_DB=1 pnpm ops:v13:subject-merge:runtime:selftest` 的 10 组 guard/事务/负向检查通过；Core 109/109、Web 914/914 与 Core/Web/专项 typecheck 通过，Web lint 为 0 error / 200 个既有 warning。
- 已验证：隔离空库不选择模板即可完成“首次设置 -> 自定义科目/阶段/任务 -> 专注收口 -> 知识证据 -> 每日复盘 -> 今日闭环”；任务详情阶段名称缺失的问题已修复并浏览器复核。390×844 与 1440×900 的今日/考试科目页面无横向溢出。
- 已验证：`pnpm check` 已通过架构/Web 治理、全包 typecheck/test、Prisma validate 和 production build；docs/risk/governance/tasks/residual 与 diff 门禁均已通过。
- 尚未完成：v1.3 签名 Release 和 production apply；本地总门禁、浏览器证据与受保护 PR/CI 不等于这些交付层级完成。

### 范围

- 首次设置支持多个自定义科目和多个分组。
- 408 去特殊化为普通可选模板包；模板确认前可编辑，确认后生成普通用户数据。
- 建立通用考试/科目/阶段模板目录和模板版本。
- 建立共享任务类型与资料分类目录；用户创建使用稳定闭集，历史 `focus` 任务仅在编辑路径兼容，系统状态枚举不开放用户 CRUD。
- 增加重复科目识别、合并预览和科目工作台；合并本版本不做物理删除。
- 清查运行时写死日期、成绩、统计、作者目标和业务科目；fixture/showcase 与真实数据隔离。

### 验收

- 空库新用户不使用任何预置科目也可完成设置、学习、收口和复盘。
- 模板未选择时不写库、不进入统计；模板选择后不保留隐藏特殊规则。
- 科目/分组 CRUD、排序、归档、恢复、影响预览和重复处理通过桌面/移动验收。

## v1.4：身份、成员与 Workspace

### 批次

1. AUTH-0：威胁模型、数据归属、迁移 preimage、邀请制和会话策略。
2. AUTH-1：账户状态、邮箱验证/密码重置、设备会话和敏感操作重新验证。
3. AUTH-2：新增 `WorkspaceMembership` 和 `WorkspaceInvitation`，现有 owner 回填为 `OWNER`。
4. AUTH-3：Workspace 创建、重命名、归档、恢复和切换；关闭/删除只进入 v1.6 数据生命周期，不在本版本物理删除。
5. AUTH-4：邀请、接受、撤销、移除、离开和所有权转移；最后一名 Owner 保护。
6. AUTH-5：全链路 workspace scope、IDOR 负向测试和旧单用户兼容。

### 用户闭环与管理面

- 账户安全中心：账户状态、密码重置、设备会话列表、撤销其他会话和敏感操作重新验证。
- Workspace 管理：创建、列表、重命名、归档、恢复、切换和当前 Workspace 明示；普通用户始终可保留个人 Workspace。
- 成员中心：邀请、待处理邀请、接受/拒绝、撤销邀请、移除成员、主动离开和所有权转移。

### 验收

- 两个用户可加入同一 Workspace；两个 Workspace 之间不能串读、串写或推断资源存在性。
- 用户可完成 Workspace 创建 -> 切换 -> 归档 -> 恢复的完整流程；归档前必须处理活动会话和最后 Owner 约束。
- 成员移除和账户冻结立即影响新请求；长操作提交时重新授权。
- 旧管理员无损成为现有 Workspace Owner，个人路径不退化。

## v1.5：RBAC 与隐私授权

### 批次

1. RBAC-0：冻结 Owner/Admin/Coach/Member/Viewer 权限矩阵和敏感数据矩阵。
2. RBAC-1：统一 policy service 和 capability DTO，API/service/query 三层接入。
3. RBAC-2：角色分配、变更和撤销管理；最后 Owner、越级授权和自我提权全部 fail closed。
4. RBAC-3：对象级分享 grant CRUD；支持仅自己、指定成员、Coach、Workspace，可到期、可撤销、可追溯。
5. RBAC-4：成员授权证据 -> Coach 建议草稿 -> 成员确认/驳回 -> 计划收件箱 -> 显式应用的协作闭环。
6. RBAC-5：权限变化、敏感读取、成员操作和拒绝结果的脱敏审计。
7. RBAC-6：角色矩阵、跨租户、批量操作和 TOCTOU（检查与使用时差）回归。

### 用户闭环与管理面

- 角色管理：列出能力、分配角色、调整角色、撤销角色并解释拒绝原因。
- 分享管理：创建、查看、修改范围/到期时间、撤销和查看访问审计；角色本身不自动获得敏感正文。
- Coach 协作：Coach 只能基于成员明确授权的最小证据生成建议草稿；成员确认后进入自己的计划收件箱，只有成员显式应用才修改正式计划。

### 验收

- 所有私有 API 有服务端授权；客户端不可信 actor/user/workspace 参数不能越权。
- 动机、情绪正文、完整复盘、私有笔记/附件、AI 输入和导出文件不因 Admin/Coach 身份自动可见。
- 角色和分享 grant 的新增、查看、修改、撤销均有服务端权限与审计；撤销后新请求立即失效。
- Coach 建议不能绕过成员确认链直接创建任务、修改阶段或扩展分享范围。

## v1.6：数据生命周期

### 批次

1. DATA-0：数据清单、owner、导出/删除/审计/备份保留矩阵和 redaction matrix。
2. DATA-1：最小持久后台任务底座，冻结 job 状态、租约、幂等、重试、取消、进度、结果和失败恢复契约；不依赖 Web 请求存活。
3. DATA-2：异步 Workspace/账户导出、版本化 manifest、checksums、短时一次性下载凭证。
4. DATA-3：数据任务中心，覆盖申请、进度、失败重试、下载、撤销下载、取消、回执和过期清理。
5. DATA-4：对象回收站、影响预览、恢复期和派生投影排除/重建。
6. DATA-5：删除预览、重新验证、冷静期、冻结、取消和 `DeletionJob` 状态机。
7. DATA-6：数据库、附件和可重建投影物理删除；失败补偿和脱敏回执。
8. DATA-7：备份删除账本；历史备份恢复后重新应用 deletion ledger，避免数据复活。

### 验收

- 导出对象、附件、manifest 和 hash 一致，不包含密码 hash、session token、Provider secret 或内部路径。
- 用户可从数据任务中心观察每次导出/删除的真实状态，取消或重试不得造成重复包、重复删除或状态回退。
- 删除预览与实际范围一致；删除后鉴权、搜索、附件、挑战和排名均不可见。
- kill-point、重试、恢复和历史备份恢复 fixture 通过后，`AF-RISK-DATA-001` 才可进入人工关闭复核。

## v1.7：受控运维中心

### 批次

1. OPS-0：只读健康、版本、agent、磁盘/证书、备份清单、维护锁和脱敏历史。
2. OPS-1：白名单 operation catalog，不接受命令文本、脚本正文、自由路径或环境变量。
3. OPS-2：expected-before、TTL、nonce、request hash、幂等、身份和审批绑定。
4. OPS-3：请求生命周期：预览、二次确认、审批、排队、取消、安全重试、hold、恢复、结果和证据历史。
5. OPS-4：root-only agent phase journal、锁、超时、崩溃恢复、reconciliation 和回滚。
6. OPS-5：检查更新、备份、签名 Release apply、允许 digest rollback、受控重启、维护模式和预定义诊断逐项开放。

### 验收

- Web runtime 仍无 Docker socket、root、生产 `.env`、备份目录和签名私钥。
- 任意 shell、未登记动作和状态漂移均 fail closed。
- 取消、重试和人工 hold 只能发生在 catalog 明确允许的状态转换中；浏览器刷新不丢失请求状态。
- 每项生产动作具备独立确认、备份/hash、smoke、回滚和 redacted evidence。

## v1.8：成长指标、私有挑战与排名

### 批次

1. RANK-0：指标定义、时区、窗口、并列、版本、隐私、退出和删除规则。
2. RANK-1：个人成长指数，只与自己的历史基准比较。
3. RANK-2：私有挑战 CRUD；创建、查看、编辑规则、开始、结束、关闭和解散均有清晰状态机。
4. RANK-3：参与者管理；邀请、主动加入/退出、移除、昵称和字段授权，以及挑战所有权转移。
5. RANK-4：可重建排名投影、异常时长、重复 session、修改/删除联动和申诉。
6. RANK-5：公开排名仅保留评审入口，不在本版本默认开放。

### 验收

- 分数带 `scoreVersion`，相同源事实可重复计算；投影故障不影响学习主链。
- 挑战从创建、成员管理、规则调整到结束/解散形成完整管理闭环；已开始的计分规则只能版本化变更，不能静默改写历史榜单。
- 动机、情绪正文、复盘、笔记/错题内容、附件、AI prompt 和私有任务标题永不进入排行。
- 退出挑战或删除账户后可重建并移除贡献。

## v1.9：平台化加固

### 批次

1. HARDEN-0：把 v1.6 最小后台任务底座扩展到导出、删除、排名重建、通知和搜索索引，补死信、人工重放和积压观测。
2. HARDEN-1：统一 Workspace、成员、通知、搜索、账户安全、数据任务和挑战入口的信息架构与跨设备状态恢复。
3. HARDEN-2：限流、配额、滥用保护、MFA/Passkey 候选和会话风险提醒。
4. HARDEN-3：举报申诉、审计检索、多租户指标/告警、容量治理和支持包脱敏。
5. HARDEN-4：大数据量、并发、恢复、灾备、桌面/移动和无障碍验收。

### 验收

- 后台任务可观测、可暂停、可重试且不会跨 Workspace 串行化错误；队列故障不阻断个人学习主链。
- 多 Workspace 和成员场景下，导航、通知、搜索结果、审计和后台任务都显示明确 scope。
- 达到容量阈值、限流、故障和降级场景时，系统给出可恢复反馈而不是静默丢任务。

## v2.0：多人学习平台门禁

v2.0 不是功能开关，而是以下证据同时成立后的发布结论：

- 个人与多人核心旅程均可完成，个人用户不被成员/排行设置干扰。
- Workspace/成员/RBAC/分享/Coach 协作、数据任务、受控运维请求和挑战管理的 CRUD 与恢复路径均已浏览器验收。
- AUTH、RBAC、EXPORT、DELETE、OPS、RANKING 六个高风险包均有独立确认与完成证据。
- 跨租户、权限、隐私、导出、删除、附件、排名和后台任务回归通过。
- 受保护 PR、CI、签名 Release、不可变 digest、production apply、smoke、回滚和观察记录齐全。
- 运营具备监控、告警、审计、事故响应、数据泄露响应和误删除恢复流程。

## 统一交付门禁

每个批次按以下顺序推进：

```text
源事实 -> 威胁/体验设计 -> 任务与确认包 -> additive migration -> 实现与专项测试
-> 浏览器/API 验收 -> 受保护 PR/CI -> 签名 Release -> 独立 production apply
-> 生产 smoke/观察 -> residual 人工复核
```

代码完成、Release 完成和生产完成是三个独立状态。任一 migration、数据删除、生产写动作、权限边界或默认数据共享变化，在执行前必须有对应批次的精确影响、风险、验证和回滚确认，不能只引用本总计划。

## 风险与回滚

- 租户隔离失败：停止多人开放，撤销邀请/成员入口，保留个人 owner 兼容路径并回滚应用。
- 权限错误：fail closed，撤销新角色能力，保留审计和受影响对象清单。
- 导出泄露：立即撤销下载 grant、清理临时包、轮换相关凭证并进入 incident 流程。
- 删除错误：执行前备份和范围 hash；处理中断进入可恢复状态，禁止静默跳过文件或关联数据。
- 运维误执行：agent 进入 hold，按 phase journal 和固定 rollback target 恢复；Web 不直接接管执行。
- 排名伤害或作弊：关闭挑战/排行投影，不影响或修改个人学习源事实。
