# 认证与安全边界

第一版采用单管理员账号。

安全要求：

- 所有写操作必须服务端校验登录会话。
- 管理员密码只保存哈希，不保存明文。
- 登录 Cookie 需要 `HttpOnly`、`Secure`、`SameSite`。
- 动机档案、情绪记录、复盘正文、错题和上传资料按高敏感数据处理。
- 日志不打印 AI key、数据库 URL、原始动机档案、完整 prompt。
- 文件上传必须限制 MIME、大小、路径和访问权限。
- 高风险操作需要二次确认和审计日志。

