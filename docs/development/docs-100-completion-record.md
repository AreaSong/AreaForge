# docs 100% 完成记录

## 目标

本文件记录 AreaForge 推进到 docs 100% 的当前证据状态。它不是目标清单，而是完成台账：只有实际代码、验证命令、烟测结果、部署状态和文档同步都存在时，才能把对应项标为完成。

`pnpm docs:completion` 会读取本文件和 `docs/development/feature-traceability.md`。它还会单独检查 Package A-E 完成行是否包含验证、烟测、文档同步和残余风险证据，并检查 Package B 的 Batch 0-6、Package D 的 D1-D5、Package E 的 E1-E4 是否全部为 `DONE / 已完成`，防止只改主状态却遗漏批次证据。当前仍有未完成项，因此该命令预期失败。

## 高风险包状态

| 包 | 当前状态 | 当前证据 | 缺口 | 下一步 |
|---|---|---|---|---|
| Package A | DONE / 已完成 | 用户已明确确认“确认执行 Package A：附件上传与鉴权访问”；已实现 noteId 绑定附件上传、鉴权下载、metadata/hash/URI 写入、`UPLOAD_DIR` 私有落盘、DB/文件补偿和 `/notes` UI；验证：`pnpm --filter @areaforge/web typecheck`、`pnpm --filter @areaforge/web lint`、`pnpm --filter @areaforge/storage test`、临时库 `pnpm db:migrate:deploy`、`pnpm --filter @areaforge/web build`、`pnpm risk:preflight`、`pnpm docs:readiness` 和 `git diff --check`；烟测：未登录上传/下载 401，PDF/PNG/JPEG/WebP 成功，多个 file、空文件、unknown magic、MIME_MISMATCH、BAD_MULTIPART、413、INVALID_DISPOSITION、文件不存在 404、hash/size 对账失败 409、软链接逃逸、补偿删除、private, no-store、nosniff、不泄露 uri/storedName/绝对路径均覆盖；页面烟测：`/notes` 上传按钮、附件列表和下载入口刷新后可见；文档同步：feature-traceability、api-surface、data-model、file-storage、notes、validation-matrix、task 和 workflow 已同步；残余风险：本批不含附件删除、错题/模拟/阶段附件、AI 解析、生产部署或孤儿文件清理 | 无 | 后续转入 Package D/E |
| Package B | DONE / 已完成 | 验证、烟测、文档同步和残余风险证据见下方 Batch 0-6 明细；用户已确认并完成 Batch 0 `StudySession` 结构化收口字段、Batch 1 `CheckIn` 日快照、Batch 2 债务事件与父子任务、Batch 3 `RecoveryState` 恢复状态、Batch 4 掌握证明条件/证据/复测记录、Batch 5 结构化模拟考试和科目结果、Batch 6 阶段计划和阶段调整草稿；临时库 migration deploy、服务级 smoke、页面 smoke、`pnpm check`、`pnpm risk:preflight` 和文档同步均纳入记录 | 无 | 后续 Package D/E 继续推进长期应用和生产发布 |
| Package C | DONE / 已完成 | 用户已明确确认“确认执行 Package C：真实 AI Provider 第一版”；已实现 OpenAI-compatible JSON provider、`chat/completions` 请求、env 配置、`AI_ENABLED=false` 本地 fallback、`AI_ENABLED=true` 显式 route 外呼、配置缺失 fallback、mock provider 成功返回 `ai_generated`、轻量限流、超时、429、401、5xx、invalid JSON、schema invalid、敏感字段拦截 provider 不被调用、标题隐私烟测 `task title may contain private content` 不进入外呼、首页普通 SSR 不触发真实 provider、日志脱敏、客户端 bundle 搜不到 AI_API_KEY、不保存完整 prompt/raw response、不发送动机档案/完整情绪记录/完整复盘正文/附件内容；验证：`pnpm --filter @areaforge/ai test`、`pnpm --filter @areaforge/ai typecheck`、`pnpm --filter @areaforge/web typecheck`、`pnpm --filter @areaforge/web lint`、`pnpm check`、`pnpm risk:preflight`、`pnpm docs:readiness`、`git diff --check`；烟测：三条 AI API 在 `AI_ENABLED=false` fallback、配置缺失 fallback、测试 provider 成功、轻量限流和失败 fallback 均覆盖；文档同步：feature-traceability、api-surface、ai-boundary、validation-matrix、task、workflow 和 completion record 已同步；残余风险：本批不含长期阶段调整 AI、自动覆盖记录、保存完整 prompt/响应或真实生产 key 烟测 | 无 | 后续转入 Package D/E |
| Package D | NOT_READY / 未完成 | 模拟考试、报告、阶段调整已有基础版；`packages/core` 已提供任务债务重排建议、债务重排应用预览、周期报告最大短板选择、短板来源/严重度/选择依据、周期报告策略、下周期草稿、报告决策只读快照、统计风险摘要、长期风险 DTO、恢复候选选择和作战地图聚合摘要纯规则；只读报告服务、`GET /api/reports/periodic` 和 `/reports` 已返回/展示 `decisionPreview` 下周期决策预览，固定 `canAutoApply=false` / `requiresUserConfirmation=true`，且只包含聚合指标、最大短板摘要、策略、草稿和确认边界；Package D Batch D1 已在用户确认后新增 `PeriodicReportDecision` additive 模型、`POST /api/reports/periodic/decisions`、`GET /api/reports/periodic/decisions`、报告确认/驳回、冻结 `reportSnapshot`、确认时保存 `nextCycleDraft`、审计写入和只读回放，且不修改任务、债务事件、阶段计划或阶段草稿；Package D Batch D2 已在用户确认后新增 `POST /api/tasks/debt-reorder/decisions`、`POST /api/tasks/debt-reorder/applications`、首页所选建议确认/驳回/应用 UI、`reorder_suggested` / `reorder_applied` 事件和审计写入，保持 `GET /api/tasks/debt-reorder` 只读，并复用 `previewTaskDebtReorderApplication` 的只处理所选项、应用前校验、小批量上限、`shouldStopOnFirstFailure` 和跳过摘要规则；统计服务、首页恢复候选、首页债务重排建议、`GET /api/tasks/debt-reorder`、`/reports` 短板追溯信息、周期策略和本地复盘草稿、首页债务重排建议只读确认标签和 `/syllabus` 分科作战地图摘要、状态分布、推荐筛选、行动类型筛选及优先节点展示已消费部分 core 规则；Package B Batch 2 已提供 `TaskDebtEvent` 事件账本供 D2 复用；`summarizeLongTermRisks` 已固定 D4 确认后的来源、时间窗口、科目/节点、证据新鲜度、下一步动作和 confirm-only DTO 口径，但当前不接 Prisma、不接 API、不写库；`pnpm risk:preflight` 已覆盖债务重排只读 API、阶段调整只读草稿 API、D1 报告决策证据门禁、D2 债务重排所选项写入口证据门禁、阶段调整 confirm-only DTO/UI、报告 confirm-only DTO、报告决策 UI 标签、报告/任务 UI 标签和文档边界；考纲服务已用已有任务、计时、笔记、错题更新时间派生证据新鲜度；`/notes` 已支持科目、节点、掌握状态和复习提醒筛选；今日任务表单已支持写入已有 `StudyTask.type`；确认范围见 `docs/development/high-risk-confirmation-packets.md`；确认前验收矩阵已写入 `docs/development/docs-100-acceptance-evidence.md` 并由 `pnpm risk:preflight` 检查 | 缺结构化复习历史、结构化遗忘风险、长期阶段调整闭环、长期 AI 草稿和 Package D 完成证据 | Package D D3-D5 仍需按确认包逐批推进 |
| Package E | NOT_READY / 未完成 | 生产发布 runbook 已写入 `docs/development/production-release-runbook.md`；确认前验收矩阵已写入 `docs/development/docs-100-acceptance-evidence.md` 并由 `pnpm risk:preflight` 检查；只读 `pnpm package-e:preflight` 已聚合检查 release 文件、compose config、Web runtime ops 禁区、Nginx 私有上传边界、Web runtime 非 migration runner 和示例 env；`docs/development/setup.md` 已明确生产 migration deploy 必须等 Package E 确认、备份点和执行载体齐备，standalone Web runtime 不能默认视为迁移执行环境 | 缺生产发布、备份、恢复演练、发布后烟测和回滚记录 | 产品能力闭环后再确认生产发布 |

## Package B 批次完成记录

完成单个批次不等于 Package B 完成。只有 Batch 0-6 全部完成、结构化读写路径和烟测证据齐全后，才能把 Package B 主行改为完成。

| 批次 | 当前状态 | 确认记录 | 验证命令 | 烟测证据 | 同步文档 | 残余风险 |
|---|---|---|---|---|---|---|
| Batch 0：`StudySession` 收口字段 | DONE / 已完成 | 用户已明确确认“确认执行 Package B Batch 0” | `pnpm db:generate`；`pnpm db:validate`；`pnpm db:migrate:diff:empty`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch0_verify pnpm db:migrate:deploy`；`pnpm --filter @areaforge/core test`；`pnpm --filter @areaforge/web typecheck`；`pnpm --filter @areaforge/web lint`；`pnpm test`；`pnpm check`；`pnpm docs:readiness`；`pnpm risk:preflight`；`git diff --check` | 临时库 seed 后完成真实 API 烟测：登录、subjects、active session、start、end、active 清空、dashboard、analytics、reports；结束计时返回 `producedNote=true`、`producedMistake=true`、`isLowConversion=true`、`closeoutVersion=1`；dashboard 返回 `latestCompletedSession`；analytics/reports 低转化计数为 2；Playwright 首页登录和同会话 reload 后仍展示质量评分、低转化、最小产出、下一步、规则原因、补产出要求和产出记录 | 已同步 data-model、api-surface、feature-traceability、implementation-order、structured-state-migration-design、validation-matrix、high-risk-confirmation-packets、completion record 和 task 状态 | Package B 已完成分批主线；历史 note 不解析、不回填；本次只验证本地临时库，不代表生产 deploy |
| Batch 1：`CheckIn` 日快照 | DONE / 已完成 | 用户已明确确认“确认执行 Package B Batch 1：CheckIn 日快照” | `pnpm db:generate`；`pnpm db:validate`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch1_verify pnpm db:migrate:deploy`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch1_smoke pnpm db:migrate:deploy`；`pnpm --filter @areaforge/core test`；`pnpm --filter @areaforge/web typecheck`；`pnpm --filter @areaforge/web lint`；`pnpm check`；`pnpm docs:readiness`；`pnpm risk:preflight`；`git diff --check` | 服务级 smoke 覆盖 active session 无任务开始后不创建 `CheckIn`、结束有效计时后生成当日快照、保存复盘后 `reviewSubmitted=true`、任务 `create/update/complete/defer/drop/recover/split/convert-review` 刷新相关学习日、模拟任务创建/完成刷新快照；手动设置今日 `CheckIn.effectiveMinutes=777/effectiveSessionCount=9/lowConversionCount=5` 后，dashboard 和 analytics 优先读快照；昨日无快照但有 31 分钟有效 session 时 analytics fallback 为 31，周报有效分钟为 808 | 已同步 data-model、feature-traceability、implementation-order、structured-state-migration-design、validation-matrix、check-in module、completion record 和 task 状态 | Package B 已完成分批主线；本批不做生产 migration deploy、不删除历史数据、不做历史回填；服务级 smoke 出现 `pg@9` 未来弃用警告但断言通过 |
| Batch 2：债务事件与父子任务 | DONE / 已完成 | 用户已明确确认“确认执行 Package B Batch 2：新增 StudyTask.parentTaskId 与 TaskDebtEvent additive migration” | `pnpm db:generate`；`pnpm db:validate`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch2_verify pnpm db:migrate:deploy`；`pnpm --filter @areaforge/core test`；`pnpm --filter @areaforge/web typecheck`；`pnpm --filter @areaforge/web lint`；`pnpm check`；`pnpm docs:readiness`；`pnpm risk:preflight`；`git diff --check`；`pnpm docs:completion` 预期失败但不再列出 Batch 2 | 服务级 smoke 覆盖 `complete/defer/drop/recover/split/convert-review/end-session` 和模拟考试完成路径：共写入 8 条 `TaskDebtEvent`，其中 `complete=3`、`defer/drop/recover/split/convert_review` 各 1；每条债务动作仍保留对应 `AuditEvent`；拆小子任务 `parentTaskId` 等于原任务 id；`GET /api/tasks/debt-reorder` 调用前后事件数不变 | 已同步 data-model、api-surface、task-debt module、tasks module、feature-traceability、implementation-order、structured-state-migration-design、validation-matrix、completion record 和 backlog 状态 | Package B 已完成分批主线；本批不做生产 migration deploy、不删除历史数据、不回填旧拆小任务父子关系、不自动应用债务重排；服务级 smoke 出现 `pg@9` 未来弃用警告但断言通过 |
| Batch 3：`RecoveryState` | DONE / 已完成 | 用户已明确确认“确认执行 Package B Batch 3：新增 RecoveryState additive migration” | `pnpm db:generate`；`pnpm db:validate`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch3_verify pnpm db:migrate:deploy`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch3_verify pnpm --filter @areaforge/web exec tsx <Batch 3 service smoke>`；`pnpm --filter @areaforge/core test`；`pnpm --filter @areaforge/web typecheck`；`pnpm --filter @areaforge/web lint`；`pnpm check`；`pnpm docs:readiness`；`pnpm risk:preflight`；`git diff --check`；`pnpm docs:completion` 预期失败但不再列出 Batch 3 | 服务级 smoke 覆盖：健康快照下 dashboard fallback 实时规则且不创建 `RecoveryState`；规则触发恢复创建 1 条 `status=active, triggerType=rule` 状态，连续两次 dashboard 不重复创建；完成规则恢复只更新 `RecoveryState.status/endedAt/exitCondition`，`StudyTask` 快照不变且 `TaskDebtEvent=0`；手动恢复创建 `triggerType=manual` 并记录 `actorId`，已有 active 时不重复创建；active 手动恢复优先于健康实时规则；取消恢复只更新 `RecoveryState`，任务快照不变；退出后 dashboard 回到实时规则 fallback；任务总数保持 2 | 已同步 data-model、api-surface、recovery-mode module、feature-traceability、implementation-order、structured-state-migration-design、validation-matrix、completion record、risk-preflight 和 backlog 状态 | Package B 已完成分批主线；本批不做生产 migration deploy、不批量修改历史欠账、不删除、不隐藏、不延期原任务；首页计时器聚焦恢复候选，任务面板保留完整任务列表 |
| Batch 4：掌握证明证据 | DONE / 已完成 | 用户已明确确认“确认执行 Package B Batch 4：掌握证明条件、证据和复测记录” | `pnpm db:generate`；`pnpm db:validate`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch4_verify pnpm db:migrate:deploy`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch4_verify pnpm --filter @areaforge/web exec tsx <Batch 4 service smoke>`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch4_retest_verify pnpm --filter @areaforge/web exec tsx <Batch 4 failed/partial retest fallback smoke>`；`pnpm --filter @areaforge/web typecheck` | 服务级 smoke 覆盖：旧节点无显式证据时仍可用任务、计时、笔记、错题 `_count` fallback 标记 `basic_exercises`；跨节点任务证据引用返回 `MASTERY_EVIDENCE_NODE_MISMATCH`；任务、计时、笔记显式证据写入后 evidence source 变为 `explicit`；`failed` 和 `partial` 复测只写入历史，不创建 `MasteryEvidence`，不压掉旧 `_count` fallback，不降低 `SyllabusNode.status/masteryLevel`，且不能被手动引用为复测证明证据；`passed` 复测后可用 `delayed_retest` 条件证明 `retest_passed`；掌握证明、证据、复测均写入 `AuditEvent` 摘要 | 已同步 data-model、api-surface、mastery-proof module、feature-traceability、structured-state-migration-design、validation-matrix、completion record 和 backlog 状态 | Package B 已完成分批主线；本批不做生产 migration deploy、不解析历史文本生成证据、不删除旧字段、不回填旧记录、复测失败或部分通过不自动降级；服务级 smoke 出现 `pg@9` 未来弃用警告但断言通过 |
| Batch 5：结构化模拟考试 | DONE / 已完成 | 用户已明确确认“确认执行 Package B Batch 5：新增 SimulationExam 与 SimulationSubjectResult additive migration” | `pnpm db:generate`；`pnpm db:validate`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch5_verify pnpm db:migrate:deploy`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch5_verify pnpm --filter @areaforge/web exec tsx <Batch 5 service smoke>`；`pnpm --filter @areaforge/core test`；`pnpm --filter @areaforge/web typecheck`；`pnpm --filter @areaforge/web lint`；`pnpm check`；`pnpm docs:readiness`；`pnpm risk:preflight`；`git diff --check`；Playwright `/simulation` 页面烟测 | 服务级 smoke 覆盖：旧 `StudyTask.type="simulation_exam"` 可由 `listSimulationTasks()` 读取；`createSimulationExam()` 创建结构化考试且不新增旧任务；`saveSimulationExamResults()` 写入考试汇总和两科结果；同一考试同一科二次保存更新原结果且 `SimulationSubjectResult` 保持 2 条；重复科目被拒绝，整场 `lossReasons` 可被清空；`listSimulationExams()` 返回结构化考试、科目结果和规则复盘 `reviewText`；创建和两次结果保存均写入 `AuditEvent`；阶段草稿仍为 `local_rule_fallback` 且无 `StagePlan` / `StageAdjustmentDraft` 表；Playwright 页面烟测覆盖登录后打开 `/simulation`、创建结构化考试、保存数学科目结果、刷新后展示 1 条结构化记录、1 科结果和规则复盘，截图见 `output/playwright/batch5-simulation.png` | 已同步 data-model、api-surface、simulation-exam module、feature-traceability、implementation-order、structured-state-migration-design、validation-matrix、completion record 和 backlog 状态 | Package B 已完成分批主线；本批不做生产 migration deploy、不自动迁移旧任务型模拟、不解析历史文本、不删除旧字段、不应用阶段计划、不外呼真实 AI；旧 simulation task 写 API 保留路由但返回 410，只读 GET 兼容旧记录 |
| Batch 6：阶段计划与调整草稿 | DONE / 已完成 | 用户已明确确认“确认执行 Package B Batch 6：阶段计划和阶段调整草稿” | `pnpm db:validate`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch6_verify_0707 pnpm db:migrate:deploy`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch6_verify_0707 pnpm --filter @areaforge/web exec tsx <Batch 6 service smoke>`；`pnpm --filter @areaforge/web typecheck`；`pnpm --filter @areaforge/web lint`；`pnpm db:validate`；`pnpm check`；`pnpm docs:readiness`；`pnpm risk:preflight`；`git diff --check`；Playwright `/simulation` 和 `/reports` 页面烟测 | 服务级 smoke 覆盖：创建 `StagePlan`，局部更新不重置 `mode/status`，非法日期合并被拒绝；生成 `StageAdjustmentDraft` 固定 `canAutoApply=false`、`requiresUserConfirmation=true`；驳回草稿后确认返回冲突；确认草稿只更新关联 `StagePlan.mode/goal/status`、草稿 `status/appliedAt/actorId` 和 `AuditEvent`；重复确认幂等；任务状态、计划日和 `TaskDebtEvent` 保持不变；临时库结果为 1 个阶段计划、2 个草稿、6 条相关审计、0 条债务事件；页面 smoke 覆盖 `/simulation` 阶段计划/草稿区和 `/reports` 持久阶段边界展示 | 已同步 data-model、api-surface、simulation-exam module、ai-stage-adjustment module、feature-traceability、implementation-order、structured-state-migration-design、validation-matrix、completion record、0013、0015、0016、0008 和 workflow 状态 | Package B 已完成分批主线；本批不做生产 migration deploy、不自动任务重排、不批量修改任务、不真实 AI 长期外呼、不删除历史阶段；Package D/E 仍需另行确认 |

## Package D 批次完成记录

完成单个批次不等于 Package D 完成。只有 D1-D5 全部完成、长期闭环写入边界、审计、smoke、文档和残余风险证据齐全后，才能把 Package D 主行改为完成。

| 批次 | 当前状态 | 确认记录 | 验证命令 | 烟测证据 | 同步文档 | 残余风险 |
|---|---|---|---|---|---|---|
| Batch D1：报告决策入口 | DONE / 已完成 | 用户已明确确认“确认执行 Package D Batch D1：报告决策入口” | `pnpm db:generate`；`pnpm db:validate`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_d1_verify_20260708080432 pnpm db:migrate:deploy`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_d1_verify_20260708080432 pnpm --filter @areaforge/web exec tsx <Package D D1 service smoke>`；`pnpm --filter @areaforge/core test`；`pnpm --filter @areaforge/web typecheck`；`pnpm --filter @areaforge/web lint`；`pnpm package-d:preflight`；`pnpm risk:preflight`；`pnpm docs:readiness`；`pnpm check`；`git diff --check` | 服务级 smoke 覆盖：确认周报创建 `PeriodicReportDecision` 和审计；重复确认返回同一 id 且 `alreadyDecided=true`；反向驳回返回冲突；驳回月报不保存 `nextCycleDraft`；决策总数 2、审计 2；`StudyTask`、`TaskDebtEvent`、`StagePlan`、`StageAdjustmentDraft` 数量保持不变；确认后新增 session，回放仍使用冻结 `reportSnapshot.effectiveMinutes=0` | 已同步 data-model、api-surface、periodic-reports module、feature-traceability、validation-matrix、completion record、0012、0013、0016、0008 和 workflow 状态 | D1 只保存报告确认/驳回、冻结快照、确认草稿和审计；本批不做任务重排、不自动改阶段计划、不外呼长期 AI、不执行生产 migration deploy；Package D 主状态仍待 D2-D5 |
| Batch D2：任务债务重排确认流 | DONE / 已完成 | 用户已明确确认“确认执行 Package D Batch D2：任务债务重排确认流” | `pnpm --filter @areaforge/web typecheck`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_d2_verify_20260708211821 pnpm db:migrate:deploy`；`DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_d2_verify_20260708211821 pnpm --filter @areaforge/web exec tsx <Package D D2 service smoke>`；`pnpm --filter @areaforge/web lint`；`pnpm --filter @areaforge/core test`；`pnpm package-d:preflight`；`pnpm risk:preflight`；`pnpm docs:readiness`；`pnpm check`；`git diff --check`；`pnpm docs:completion` 预期失败但不再列出 Batch D2 | 服务级 smoke 覆盖：确认所选建议只写 `TaskDebtEvent.action=reorder_suggested` 和 `AuditEvent`，任务与 `StagePlan` / `StageAdjustmentDraft` 不变；驳回所选建议同样只记账不改任务；应用所选补做任务只修改所选任务并写 `reorder_applied` 与审计；多选存在过期项时按 `shouldStopOnFirstFailure` 停止写入并返回跳过摘要；未选择的欠账任务不被延期、删除或拆分；`GET /api/tasks/debt-reorder` 仍为 GET-only 只读建议；临时库结果 `reorder_suggested=2`、`reorder_applied=1`、审计 4、任务 2、阶段计划 1、阶段草稿 1、migration 10 条 | 已同步 api-surface、task-debt module、feature-traceability、validation-matrix、second-stage design、completion record、0008、0016 和 workflow 状态 | D2 不新增 migration、不执行生产 migration deploy、不自动应用全部建议、不修改 `StagePlan` / `StageAdjustmentDraft`、不外呼长期 AI；Package D 主状态仍待 D3-D5 |
| Batch D3：长期阶段 AI 草稿 | NOT_READY / 未完成 | 待用户明确确认“确认执行 Package D Batch D3：长期阶段 AI 草稿” | 待确认后运行 | 待确认后补充 | 待确认后同步 | 未确认前不得外呼长期 AI、发送完整正文/附件内容、保存完整 prompt/响应或自动应用阶段计划 |
| Batch D4：长期风险和主题闭环补强 | NOT_READY / 未完成 | 待用户明确确认“确认执行 Package D Batch D4：长期风险和主题闭环补强” | 待确认后运行 | 待确认后补充 | 待确认后同步 | 未确认前不得新增未确认 migration、自动覆盖任务或阶段计划 |
| Batch D5：Package D 收口 | NOT_READY / 未完成 | 待用户明确确认“确认执行 Package D Batch D5：Package D 收口” | 待确认后运行 | 待确认后补充 | 待确认后同步 | D5 只收口 Package D，不执行 Package E 生产部署 |

## Package E 批次完成记录

完成单个批次不等于 Package E 完成。只有 E1-E4 全部完成、发布、备份、恢复演练、发布后烟测、回滚和残余风险证据齐全后，才能把 Package E 主行改为完成。

| 批次 | 当前状态 | 确认记录 | 验证命令 | 烟测证据 | 同步文档 | 残余风险 |
|---|---|---|---|---|---|---|
| Batch E1：生产配置与发布工件预检 | NOT_READY / 未完成 | 待用户明确确认“确认执行 Package E Batch E1：生产配置与发布工件预检” | 待确认后运行 | 待确认后补充 | 待确认后同步 | 未确认前不执行生产部署、不运行生产 migration、不触碰生产数据库或上传目录 |
| Batch E2：发布前备份与恢复演练 | NOT_READY / 未完成 | 待用户明确确认“确认执行 Package E Batch E2：发布前备份与恢复演练” | 待确认后运行 | 待确认后补充 | 待确认后同步 | 未确认前不生成真实生产备份、不覆盖生产库、不自动修复 metadata 或移动上传文件 |
| Batch E3：生产发布与 migration deploy | NOT_READY / 未完成 | 待用户明确确认“确认执行 Package E Batch E3：生产发布与 migration deploy” | 待确认后运行 | 待确认后补充 | 待确认后同步 | 未确认前不执行生产发布、生产 migration deploy、Nginx/compose 切换或发布后真实烟测 |
| Batch E4：回滚演练与 Package E 收口 | NOT_READY / 未完成 | 待用户明确确认“确认执行 Package E Batch E4：回滚演练与 Package E 收口” | 待确认后运行 | 待确认后补充 | 待确认后同步 | E4 不新增网页内一键更新或服务器命令入口 |

## 最近确认前护栏补强

- Package A：已从确认前护栏切换为完成后证据门禁；`pnpm risk:preflight` 继续阻止 `public/` 上传目录和 UI 泄露 `Attachment.uri` / `upload://attachment`，同时要求已确认完成后存在上传/下载 route、附件 service、Web 上传 IO、`/notes` 文件选择和鉴权 `downloadApiPath`；`AttachmentDto` 只暴露鉴权 API 路径，不把内部 `uri`、`storedName` 或上传绝对路径传给 UI；附件内容默认不进入 AI 上下文。
- Package B：Batch 6 已在确认后完成阶段计划和阶段调整草稿；`StagePlan`、`StageAdjustmentDraft` additive migration、服务/API/DTO/UI 主写入路径和确认边界 smoke 均已落地；`pnpm risk:preflight` 已补充 Batch 6 运行时边界，要求对应 migration、schema 字段、service、API、DTO、UI 和确认边界证据，并仅狭窄放行 `stage-adjustment-drafts/:id/confirm|reject`，避免与 Package D 未确认写路由禁区冲突；`pnpm docs:completion` 已增强为要求每个 DONE 批次具备确认、验证命令、烟测、文档同步和残余风险证据；`GET /api/tasks/debt-reorder` 仍保持只读，不写 `reorder_suggested/reorder_applied`；D2 后确认/驳回/应用记录只通过 Package D 的所选项写入口执行。
- Package C：已从确认前护栏切换为完成后证据门禁；`pnpm risk:preflight` 允许已确认的 OpenAI-compatible provider 和 Web 服务端 env 读取，但要求三条 AI route 保持鉴权 POST-only、只有 `allowExternalProvider=true` 的显式 route 调用能外呼、轻量限流存在、首页普通 SSR 继续本地 fallback、客户端无 `NEXT_PUBLIC_AI*`、`AI_LOG_PROMPTS=false` / `AI_ALLOW_SENSITIVE_CONTEXT=false` 默认值不变、上下文保持聚合最小化、任务标题脱敏、客户端 bundle key scan command 保留，以及 `AiCall` / `AiUsage` / `tokenUsage` / `promptHash` 等调用历史或费用统计持久化禁区继续有效。
- Package D：`pnpm risk:preflight` 已显式检查 `GET /api/reports/periodic` 只读、`GET /api/tasks/debt-reorder` 只读、`GET /api/simulation/stage` 只读，继续阻止未确认的报告、债务、阶段和模拟考试 `apply/confirm/reject` 写路由，并把报告快照、报告决策、报告应用、任务重排应用、阶段调整应用、阶段计划应用和长期 AI 建议等持久化 token 扫描扩展到 `apps/web/lib/study/**`；同时 `pnpm package-d:preflight` 聚合检查 D1-D5 确认句、completion record 锁定状态、报告/债务/阶段 route、未确认持久化禁区、长期 AI 禁区和 core 纯规则前置；Package D 批次感知护栏已具备：D1 完整证据后狭窄放行报告决策入口，D2 完整证据后狭窄放行债务重排所选项确认/驳回/应用入口，D3 完整证据前不放行长期阶段 AI 草稿触发面，D4 完整证据前不强行声称长期风险/主题闭环已接入页面/API，D5 完整证据前不允许 Package D 主状态完成；D2 已用 `previewTaskDebtReorderApplication` 锁住所选项、小批量上限、应用前校验、`shouldStopOnFirstFailure` 和跳过摘要；阶段调整草稿创建在 D3 确认前继续被门禁锁定为 `source="local_rule"`，不得出现 `source="ai"` 写路径；解锁必须同时具备 DONE、明确确认、`pnpm` 验证、烟测、已同步文档和残余风险证据；长期闭环设计补充依赖-允许能力矩阵和只读回归要求。
- Package E：确认前 compose 校验已统一为 `docker compose --env-file .env.example -f docker-compose.prod.yml config`，并新增只读 `pnpm package-e:preflight` 聚合检查 release 文件、compose、Web runtime ops 禁区、Nginx 私有上传边界、示例 env、E1-E4 completion ledger、runbook 批次契约和 root `package.json` 运维脚本禁区；新增只读 `pnpm release:evidence:validate <release-record.txt> [attachment-reconciliation.csv]` 校验发布记录字段、hash、枚举、敏感值泄露和附件对账 `report_only` 边界；`docs/development/setup.md` 已补充生产 migration deploy 必须等 Package E 确认、备份点和执行载体齐备，且 standalone Web runtime 不能默认视为迁移执行环境；裸跑生产 compose 若没有生产 env，预期会因 `AUTH_SESSION_SECRET is required` 等 required production env 缺失而失败；runbook 补充发布中止条件和恢复演练验收判定表，`pnpm risk:preflight` 继续阻止网页 API 触发 deploy/backup/restore/migration，并同步检查 E1-E4 ledger 与 root scripts 运维禁区。

## 当前阻塞完成的功能状态

以 `docs/development/feature-traceability.md` 为准，当前仍存在：

- 第一版必须项：`知识点掌握证明基础版` 已用现有任务、计时、笔记、错题证据和显式条件/证据/复测记录闭环；`/syllabus` 可选择目标掌握等级、保存条件、写入证据引用和复测记录，`PATCH /api/syllabus/nodes/:id` 校验证据后写入 `SyllabusNode.status/masteryLevel` 和 `AuditEvent` 摘要；没有显式证据时仍保留 `_count` fallback。`笔记与资料上传` 已由 Package A 完成 noteId 附件上传、私有落盘、鉴权下载和 `/notes` UI；`鞭策文案`、`AI 复盘建议`、`AI 明日任务建议` 已由 Package C 完成真实 provider 第一版。
- 第二阶段增强：`全真模拟考试模式完整实现` 已由 Package B Batch 5 完成结构化模型/API/UI 主路径，并由 Batch 6 补齐阶段计划和持久草稿确认边界；Package D Batch D1 已完成周/月报告确认、驳回、冻结快照、确认草稿、审计和只读回放；Package D Batch D2 已完成任务债务重排确认、驳回和所选项应用记录；`AI 根据长期数据生成阶段调整建议` 仍只有本地规则草稿，真实长期 AI 与更完整应用闭环需 Package D / `0017`。`状态主题深度联动` 已由 core 五态规则、首页状态主题面板、恢复态任务聚焦、冲刺任务前置和页面可读性烟测覆盖，深层阶段计划主题信号后续随 Package D 增强。
- Package A、Package B、Package C、Package D Batch D1 和 Package D Batch D2 已达到对应完成证据要求；Package D 主包和 Package E 仍未达到完成证据要求。

因此当前准确进度是：治理、确认包、Package A 附件、Package B Batch 0-6、Package C 真实 AI 第一版、Package D Batch D1 报告决策入口、Package D Batch D2 任务债务重排确认流和大量基础能力已完成；docs 100% 尚未完成，下一条高风险实现主线应从 Package D D3-D5 或 Package E 生产发布中按确认顺序推进。

## 最近验证

- `pnpm docs:readiness`：通过，说明治理结构和追踪入口存在；脚本已检查 `docs:completion` 具备 Package B、Package D 和 Package E 批次明细证据门禁；也检查 `risk:preflight` 保留 D1-D3 完整证据解锁、写路由扫描、长期 AI 路由扫描和持久化 token 护栏。
- `pnpm docs:completion`：预期失败，当前 blocker 为第二阶段的长期 AI 阶段调整、Package D/E 主完成证据，以及 Package D D3-D5 / Package E E1-E4 批次完成证据。
- `pnpm check`：通过，说明当前工程基线可构建；本次覆盖 Prisma 客户端生成、全 workspace typecheck、Web lint、Prisma validate 和 Next build。
- `DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch4_verify pnpm db:migrate:deploy`：通过，7 条 migration 已在本地临时库成功应用。
- Batch 4 服务级 smoke：通过，旧 `_count` fallback、跨节点证据拒绝、显式证据优先、`failed/partial` 复测不降级、不计入通过、不创建显式证据且不压掉 fallback、`passed` 复测证明 `retest_passed` 和审计摘要写入均已覆盖。
- `pnpm --filter @areaforge/core test`：通过，当前 53 条 core 规则测试覆盖 Batch 1 `buildDailyCheckInSnapshot` 快照字段来源、低转化 fallback 和显式 `isLowConversion=false` 覆盖优先级、状态主题五态映射、恢复态任务裁剪、冲刺任务前置、周期报告最大短板选择、短板来源/严重度/选择依据、周期策略、任务债务重排、D4 长期风险 DTO、恢复候选、统计风险、掌握证明条件与证据门禁、作战地图和模拟/阶段规则。
- `pnpm --filter @areaforge/ai test`：通过，当前 18 条 AI 测试覆盖本地 fallback、mock provider 成功/失败、非法输出 fallback、敏感字段拦截、安全最小化上下文、OpenAI-compatible `chat/completions`、`ai_generated`、超时/429/401/5xx/invalid JSON/schema invalid fallback、`task title may contain private content` 标题隐私烟测和 prompt 最小化；Web 层补充轻量限流，超限时不调用 provider 并 fallback。
- `pnpm --filter @areaforge/storage test`：通过，当前 11 条 storage 测试覆盖 MIME/magic bytes、大小限制、随机 storedName/URI、路径穿越、公有目录拒绝和私有下载响应头。
- Package A route-level smoke：通过，覆盖未登录上传/下载 401、创建 note、缺 file 400、多 file 400、空文件 400、unknown magic 400、MIME_MISMATCH 400、超大 413、PDF/PNG/JPEG/WebP 上传成功、响应不泄露 uri/storedName/上传绝对路径、原始名 `../evil.pdf` 归一化、下载 200、`private, no-store`、`nosniff`、`Content-Disposition`、`Content-Length`、inline 成功、INVALID_DISPOSITION 400、`GET /api/notes` 刷新后附件可见、文件缺失 404、文件篡改后 hash/size 对账失败 409。
- Package A 软链接/public 上传根烟测：通过，`UPLOAD_DIR` 指向 `apps/web/public` 的软链接时上传返回 `UPLOAD_DIR_UNSAFE`，metadata 数不变。
- Package A `/notes` 页面烟测：通过，生产 build 后 `next start` 临时端口登录打开 `/notes`，页面展示上传按钮、4 个附件和下载入口，截图为 `output/playwright/package-a-notes.png`；同一临时库首页曾出现连接数打满的 `P2037`，目标页流程通过，残余风险记录为临时烟测环境连接残留。
- `pnpm test`：通过，说明当前所有 workspace 包内测试通过。
- `pnpm --filter @areaforge/web typecheck`：通过。
- `pnpm --filter @areaforge/web lint`：通过。
- `DATABASE_URL=postgresql://areaforge:areaforge@127.0.0.1:54330/areaforge_batch5_verify pnpm db:migrate:deploy`：通过，8 条 migration 已在本地临时库成功应用。
- Batch 5 服务级 smoke：通过，结构化模拟创建不新增旧任务、旧任务型模拟只读可查、两科结果保存和同科 upsert 唯一性、规则复盘文本、审计事件、Batch 6 表未出现均已覆盖。
- Batch 5 Playwright 页面烟测：通过，`/simulation` 可创建结构化考试、保存科目结果并刷新展示总分、科目结果和规则复盘；截图为 `output/playwright/batch5-simulation.png`。烟测期间首页曾因临时库连接数打满返回 `P2037`，目标页面流程重试后通过。
- Batch 6 服务级 smoke：通过，阶段计划创建和局部更新、非法日期拒绝、持久草稿生成、驳回后确认冲突、确认应用、重复确认幂等、审计记录和任务不变边界均已覆盖；临时库结果为 1 个阶段计划、2 个草稿、6 条相关审计、0 条债务事件。
- Package D Batch D1 服务级 smoke：通过，确认周报创建 1 条 `PeriodicReportDecision` 和 1 条审计；重复确认返回同一 id 且 `alreadyDecided=true`；反向驳回返回冲突；驳回月报创建 rejected 决策且不保存 `nextCycleDraft`；决策总数为 2、审计总数为 2；`StudyTask`、`TaskDebtEvent`、`StagePlan`、`StageAdjustmentDraft` 数量保持不变；确认后新增 session，回放仍使用冻结 `reportSnapshot.effectiveMinutes=0`。
- Package D Batch D2 服务级 smoke：通过，确认所选建议写入 `reorder_suggested` 和审计且不修改任务/阶段；驳回所选建议只记账；应用所选补做任务写入 `reorder_applied` 和审计且只修改所选任务；带过期项的小批量按 `shouldStopOnFirstFailure` 停止写入并返回跳过摘要；未选择任务不被延期、删除或拆分；`StagePlan`、`StageAdjustmentDraft` 不变，未新增 migration；临时库输出 `suggestionAction=recover`、`reorder_suggested=2`、`reorder_applied=1`、审计 4、任务 2、阶段计划 1、阶段草稿 1。
- `pnpm package-d:preflight`：通过；该命令检查 Package D D1-D5 确认句、completion record 锁定状态、报告/债务/阶段 route、未确认持久化禁区、长期 AI 禁区和 core 纯规则前置，不写库、不外呼长期 AI；D1 完整证据后仅狭窄放行报告决策入口，D2 完整证据后仅狭窄放行债务重排所选项写入口，D3-D5 继续锁定。
- `pnpm risk:preflight`：通过后说明 Package A、Package B、Package C、Package D D1 和 Package D D2 的实现证据存在；Package C 已允许真实 provider 代码和服务端 env 读取，但继续要求首页不触发外呼、三条 AI route 鉴权 POST-only、客户端无公开 AI env、敏感上下文不发送、prompt/raw response 不持久化；Package D D3-D5/E 的确认前边界仍被拦住。
- `pnpm docs:completion`：预期失败；当前 blocker 为第二阶段长期 AI 阶段调整、Package D/E 主完成证据，以及 Package D D3-D5 / Package E E1-E4 批次完成证据；脚本已增强为每个高风险批次必须同时具备确认、验证命令、烟测、文档同步和残余风险证据，Package A/C 完成行必须具备专项证据。
- `docker compose config`：通过；`docker compose --env-file .env.example -f docker-compose.prod.yml config`：通过。确认前只用于本地结构校验，不执行生产部署。
- `pnpm package-e:preflight`：通过；该命令只读本地 release artifact 并执行 compose config，确认 Web 绑定 localhost、PostgreSQL 不暴露公网端口、上传目录不被 Nginx 静态暴露、Web runtime 不假装具备 migration runner 能力，且 Web runtime 不存在 deploy/backup/restore/migration 命令执行面，不执行生产部署、备份、恢复或 migration。
- `git diff --check`：通过。

## 更新规则

- 每完成一个高风险包，必须把对应行改为“完成”，并在“当前证据”中写入验证命令、烟测证据、文档同步结果和残余风险；`pnpm docs:completion` 会检查这些关键词。
- 每完成一个 Package B 批次，也必须追加批次证据；`pnpm docs:completion` 会要求 Batch 0-6 全部为 `DONE / 已完成`；在 Batch 0-6 全部完成前，不得把 Package B 主状态改成完成。
- 每完成 Package D 或 Package E 的一个批次，也必须追加对应 D1-D5 或 E1-E4 批次证据；在所有批次完成前，不得把 Package D 或 Package E 主状态改成完成。
- 若只完成低风险基础版，不得把高风险包标为完成。
- 任何“未验证”“待确认”“未完成”的包都会阻止 `pnpm docs:completion` 通过。
